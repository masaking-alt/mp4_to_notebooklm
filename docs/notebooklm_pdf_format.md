# NotebookLM用PDF形式

`動画ファイル名.pdf` は1スライドを1ページ以上で表現します。

各スライドページには以下を含めます。

- スライド番号
- 時間範囲
- 画像ファイル名
- スライド画像
- 文字起こし
- OCRテキスト

NotebookLMでテキストを抽出できるよう、PDFには画像だけでなく文字起こしとOCRのテキストを埋め込みます。

スライド画像名は `動画ファイル名_001.png` の形式です。
